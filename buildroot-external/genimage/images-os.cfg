image kernel.img {
	size = ${KERNEL_SIZE}
	srcpath = "${BINARIES_DIR:-PLEASE_SPECIFY_BINARIES_DIR}/${KERNEL_FILE:-PLEASE_SPECIFY_KERNEL_FILE}"

	squashfs {
		compression = "lzma"
		block-size = 131072
	}
}
